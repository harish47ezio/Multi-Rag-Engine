# Copy this file to `.env` and fill in your real values.
# `.env` is gitignored and cursorignored, so your real secrets stay local.

# Ollama Cloud API key. Get one from: https://ollama.com/settings/keys
OLLAMA_API_KEY=ollama_replace_me_with_real_key
